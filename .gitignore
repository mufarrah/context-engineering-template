venv
venv_linux
__pycache__
.ruff_cache
.pytest_cache

# Maintainer research notes (local only, never shipped)
.research/

# Maintainer-local agent context. Repo root ONLY — the leading slash is
# required so the shipped copies under templates/ stay tracked.
/CLAUDE.md
/AGENTS.md